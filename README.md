# VM-Docs

VM文档库, 基于vitepress开发

## 开发/部署

本项目可以使用VSCode（或VSCodium及其基于此项目的发行版）、WebStorm进行开发

开发前请先安装[NodeJS](https://nodejs.org/)最新的LTS版本

安装依赖：

```sh
pnpm i
```

开发预览：

```sh
pnpm run docs:dev
```

构建：

```sh
pnpm run docs:build
```